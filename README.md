1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?

Ans : 1. getElementById : it select only a specified unique ID . it is the fast way to select an element.

     2. getElementByClassName : it select all element that have the specified class name. 

     3. querySelector : it select all the valid css selector like id, class, tag etc.



2.  How do you create and insert a new element into the DOM?

Ans : to create and insert a new element into the DOM we need to first crete the new element and then use appendChild() to add it. 


3. What is Event Bubbling? And how does it work?

Ans: when an event happens on an element like click on a button, that event bubbles up to its parent elements, then it's grandParent. 


4. What is Event Delegation in JavaScript? Why is it useful?

Ans: Event delegation is a design pattern in JavaScript for handling events efficiently.


5. What is the difference between preventDefault() and stopPropagation() methods?
Ans: 1.preventDefault(): This method is used to cancel the default behavior attached with an event.
    2. stopPropagation() : This method is used to control the flow of an event through the DOM tree. 
